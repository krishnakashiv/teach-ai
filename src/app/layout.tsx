export const metadata = {
  title: "Teach AI",
  description: "Chatbot for intergrated learning",
};

import "../global.css";

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
